---
---

Add `previews/<preset>/vite.config.ts` and `pnpm preview:<preset>` scripts to
view each lint preset's effective rules with vite-plus-inspector. Dev tooling
only — not part of the published package, so no release.
