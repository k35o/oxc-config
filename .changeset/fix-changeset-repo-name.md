---
---

Fix the repo name in `.changeset/config.json` so changelog generation can resolve PRs against `k35o/oxc-config` (was incorrectly pointing at `k35o/oxlint-config`). No published behavior change.
