---
'@k35o/oxc-config': minor
---

Initial release. Strict, layered oxlint configurations for `base` / `typescript` / `react` / `nextjs` / `backend` / `test` / `tailwind` plus an opinionated oxfmt preset (`fmt`). Composable through oxlint's native `extends`. Built and dogfooded with vite-plus.
